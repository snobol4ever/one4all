*  f20: &ALPHABET — 256-char extended ASCII (scrip-cc), not full Unicode
        IDENT(SIZE(&ALPHABET), 256)             :F(FAIL)
        OUTPUT = 'PASS'                         :(END)
FAIL    OUTPUT = 'FAIL'
END
