/* sm_codegen_x64_emit.c — stub TU.
 * All implementation is in emit_sm.c (included in libscrip_rt.so).
 * This file exists so gate harnesses that list it explicitly on the gcc
 * command line do not get a "no input files" error. */
#include "sm_codegen_x64_emit.h"
