/* sm_emit_template.c — stub TU.
 * Template rendering (render_macro_body, render_call_line, build_args_col)
 * lives in emit_sm.c and is included in libscrip_rt.so.
 * This file exists so gate harnesses that list it on the gcc command line
 * do not get a "no input files" error. */
#include "emit_sm.h"
